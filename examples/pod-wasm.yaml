apiVersion: boss.io/v1
kind: Pod
metadata:
  name: demo-wasm
  namespace: default
  labels:
    app: demo-wasm
spec:
  sandboxClass: wasm
  sandbox:
    artifact:
      type: wasmModule
      path: /tmp/demo.wasm
    isolation: runtimeSandboxed
    network:
      mode: none
  containers:
    - name: app
      args: ["--serve"]
  restartPolicy: Never
